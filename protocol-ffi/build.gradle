// module1/build.gradle
apply plugin: 'maven-publish'

// 将预编译的AAR发布到Maven仓库
publishing {
    publications {
        aar(MavenPublication) {
            // 不指定groupId/artifactId，由JitPack自动生成
            artifact file("$projectDir/protocol-ffi-4.2.7.aar")
        }
    }
}